// to start node just type node in the terminal and press enter
// to stop node just press ctrl + c or d
// to restart node just type node in the terminal and press enter
// to see the modules that are available in node press tab key couple of time  the modules will be displayed
// to see the methods that are available in a module type the module name fallow by a dot and press tab key a couple of times the methods will be displayed

//SALAD

/// END OF SALAD
// console.log(newArrayOfObj.length);

// let newlist = newArrayOfObj.map(function (element) {
//   element.image = element.id;
//   return element;
// });

// // console.log(newlist);
// console.log(newlist.length);
